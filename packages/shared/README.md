# `packages/shared`

Shared TypeScript types, enums, and API contract helpers used by `apps/web` and (via generated JSON Schema / OpenAPI) aligned with Python DTOs.

## Status

Placeholder until Phase 1 scaffolding. Will hold:

- Entitlement event category enums
- Epistemic status types
- Notice / review status unions
- Job payload types

Keep this package free of Next.js or React dependencies.
